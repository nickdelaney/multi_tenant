%h1= @section.name
%p= @section.description
%h4= "Class Size: #{@section.class_size}"

= link_to "Edit", edit_section_path(@section)
= link_to "Destroy", section_path(@section), method: :delete, data: { confirm: "Are you sure?"}
= link_to "Home", root_path


%h2 Students

